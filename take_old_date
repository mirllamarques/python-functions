#pega a data mais antiga de uma lista de datas
def take_old_data(lista,tamanho):
  menor_data = lista[0][0]
  for i in range(1,tamanho):
    if(lista[i][0] < menor_data):
      menor_data = lista[i][0]
  return menor_data
